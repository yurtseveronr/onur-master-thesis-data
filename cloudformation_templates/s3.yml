AWSTemplateFormatVersion: '2010-09-09'
Description: 'KMS + S3 for dataset storage with Amazon Personalize permissions'

Resources:
  # KMS Key
  DatasetKMSKey:
    Type: AWS::KMS::Key
    Properties:
      KeyPolicy:
        Statement:
          # Root account permissions
          - Effect: Allow
            Principal:
              AWS: !Sub 'arn:aws:iam::${AWS::AccountId}:root'
            Action: 'kms:*'
            Resource: '*'
          # Amazon Personalize permissions
          - Effect: Allow
            Principal:
              Service: personalize.amazonaws.com
            Action:
              - 'kms:Decrypt'
              - 'kms:GenerateDataKey'
              - 'kms:GenerateDataKey*'
              - 'kms:CreateGrant'
              - 'kms:DescribeKey'

            Resource: '*'
          - Effect: Allow
            Principal:
              AWS: arn:aws:iam::708778582346:role/service-role/aws-quicksight-service-role-v0
            Action:
              - 'kms:Decrypt'
              - 'kms:GenerateDataKey'
              - 'kms:GenerateDataKey*'
              - 'kms:CreateGrant'
              - 'kms:DescribeKey'
            Resource: '*'

  # KMS Key Alias for easier reference
  DatasetKMSKeyAlias:
    Type: AWS::KMS::Alias
    Properties:
      AliasName: alias/onur-master-key
      TargetKeyId: !Ref DatasetKMSKey

  # S3 Bucket
  DatasetBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: onur-master-dataset
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: 'aws:kms'
              KMSMasterKeyID: !Ref DatasetKMSKey
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true

  
  DatasetBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: !Ref DatasetBucket
      PolicyDocument:
        Statement:
          - Effect: Allow
            Principal:
              Service: personalize.amazonaws.com
            Action:
              - 's3:GetObject'
              - 's3:ListBucket'
            Resource:
              - 'arn:aws:s3:::onur-master-dataset'
              - 'arn:aws:s3:::onur-master-dataset/*'
          - Effect: Allow
            Principal:
              AWS: arn:aws:iam::708778582346:role/service-role/aws-quicksight-service-role-v0
            Action:
              - 's3:GetObject'
            Resource: 'arn:aws:s3:::onur-master-dataset/*'

          - Effect: Allow
            Principal:
              AWS: arn:aws:iam::708778582346:role/service-role/aws-quicksight-service-role-v0
            Action:
              - 's3:PutObject'
              - 's3:PutObjectAcl'
            Resource: 'arn:aws:s3:::onur-master-dataset/*'

          - Effect: Allow
            Principal:
              Service: personalize.amazonaws.com
            Action:
              - 's3:PutObject'
              - 's3:PutObjectAcl'
            Resource:  'arn:aws:s3:::onur-master-dataset/*'

Outputs:
  BucketName:
    Description: 'S3 Bucket name for datasets'
    Value: !Ref DatasetBucket
    Export:
      Name: !Sub '${AWS::StackName}-BucketName'
  
  KMSKeyId:
    Description: 'KMS Key ID for encryption'
    Value: !Ref DatasetKMSKey
    Export:
      Name: !Sub '${AWS::StackName}-KMSKeyId'
  
  KMSKeyArn:
    Description: 'KMS Key ARN'
    Value: !GetAtt DatasetKMSKey.Arn
    Export:
      Name: !Sub '${AWS::StackName}-KMSKeyArn'